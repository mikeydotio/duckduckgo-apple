#!/bin/bash
# Build an iOS scheme or local package. Usage: iOS/scripts/build <target> [extra xcodebuild args]
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "${SCRIPT_DIR}/../../scripts/build-scripts/core" build ios "$@"
